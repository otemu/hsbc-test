import {
  CommentNode,
  StepNode,
  StartNode,
  EndNode,
  DecisionNode,
} from '../components/CustomNodes';

export const getCustomNodeTypes = {
  commentNode: CommentNode,
  stepNode: StepNode,
  startNode: StartNode,
  endNode: EndNode,
  decisionNode: DecisionNode,
};
